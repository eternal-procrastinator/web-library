import styled from "styled-components";
import { Button } from "antd";

export const StyledButton = styled(Button)`
    padding-inline: 32px;
    padding-block: 8px;
    font-size: 18px;
    height: auto
`;
